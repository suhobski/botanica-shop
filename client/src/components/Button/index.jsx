import styled from "styled-components";

export const Button = styled.button`
  background: transparent;
  border: none;
  border-radius: 0.3em;
  box-shadow: none;
  cursor: pointer;
  font-size: 1em;
`;
